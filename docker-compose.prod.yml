version: '3.8'

# Prod compose scaffold
services:
  backend:
    build:
      context: ./apps/backend
      dockerfile: ../../docker/backend.Dockerfile
  frontend:
    build:
      context: .
      dockerfile: docker/frontend.Dockerfile
